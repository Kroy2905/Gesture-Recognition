<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent">

    <androidx.camera.view.PreviewView
        android:id="@+id/view_finder"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"
        app:scaleType="fillStart" />

    <com.google.mediapipe.examples.gesturerecognizer.OverlayView
        android:id="@+id/overlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- RelativeLayout to hold TextView and Switch -->
    <RelativeLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|end"
        android:layout_marginTop="16dp"
        android:layout_marginEnd="16dp">

        <!-- TextView to the left of Switch -->
        <TextView
            android:id="@+id/text_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:padding="5dp"
            style="@style/TextAppearance.Material3.BodyLarge"
            android:textAppearance="@anim/nav_default_enter_anim"
            android:text="Detect Gesture"
            android:textSize="15dp"
            android:background="@color/white"
            android:textColor="@color/hand_line_color"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"/>

        <!-- Switch to the right of TextView -->
        <Switch
            android:id="@+id/gesture_switch"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_toEndOf="@id/text_view"
            android:layout_centerVertical="true"
            android:thumbTint="#E6EAED"
            android:trackTint="@color/white"
            android:switchPadding="5dp"
            tools:ignore="UseSwitchCompatOrMaterialXml" />
    </RelativeLayout>
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        >
        <TextView
            android:id="@+id/gesture_Text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintBottom_toTopOf="@+id/gesture_image"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_marginHorizontal="10dp"
            android:padding="10dp"
            style="@style/TextAppearance.Material3.BodyLarge"
            android:textAppearance="@anim/nav_default_enter_anim"
            android:text="-----"
            android:textSize="15dp"
            android:background="@color/white"
            android:textColor="@color/hand_line_color"
            android:layout_alignParentStart="true"
            android:layout_centerVertical="true"

            />
        <ImageView
            android:padding="5dp"
            android:id="@+id/gesture_image"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:visibility="invisible"

            />

    </androidx.constraintlayout.widget.ConstraintLayout>



</androidx.coordinatorlayout.widget.CoordinatorLayout>
